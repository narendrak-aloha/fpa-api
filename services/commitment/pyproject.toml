[project]
name = "commitment-service"
version = "0.1.0"
description = "Standalone, deliberately unreliable downstream commitment ledger."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
