# Every dependency the application needs. The container installs these at build time
# and re-checks them on every start (see docker/entrypoint.sh).
# Install:  pip install -r requirements.txt

# --- web service -------------------------------------------------------------
fastapi>=0.115
uvicorn>=0.24
pydantic>=2.0

# --- ClickHouse cube: query execution and the seed generator -----------------
clickhouse-connect>=0.7
faker>=40.0
numpy>=2.0

# --- Postgres governance store: models, migrations, YAML seed ----------------
alembic>=1.16
sqlalchemy>=2.0
psycopg[binary]>=3.1
pyyaml>=6.0

# --- agent team and model providers ------------------------------------------
agno>=1.0
google-genai>=1.0          # provider: gemini
anthropic>=0.40            # provider: claude-api
claude-agent-sdk>=0.2      # provider: claude-code (also needs the local `claude` CLI)

# --- used by the startup requirement check ------------------------------------
packaging>=23.0
